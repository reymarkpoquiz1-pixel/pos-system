name: pos
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  image_picker: ^1.1.2
  intl: ^0.19.0
  fl_chart: ^0.66.0
  shared_preferences: ^2.3.2
  pdf: ^3.10.8
  printing: ^5.11.1
  mobile_scanner: ^7.3.0
  esc_pos_utils_plus: ^2.0.3
  connectivity_plus: ^6.0.3
  
  # Para sa local database/offline storage (sqflite)
  sqflite: ^2.4.1
  path_provider: ^2.1.1
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/larawan.png
